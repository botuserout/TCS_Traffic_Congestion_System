{
  "name": "tcs-traffic-congestion-system",
  "version": "1.0.0",
  "description": "Real-time Traffic Congestion System using YOLOv8 and ByteTrack",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n BACKEND,FRONTEND -c bgBlue.bold,bgGreen.bold \"python backend/server.py\" \"npm --prefix frontend run dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev:frontend",
    "dev:backend": "python backend/server.py",
    "install:all": "pip install -r backend/requirements.txt && npm --prefix frontend install",
    "install:frontend": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
